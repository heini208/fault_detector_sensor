# Firmware interface mirror

This minimal package lets `micro_ros_platformio` generate type support for the
ESP32. `srv/SetSensorAcquisition.srv` must remain byte-for-byte identical to the
canonical definition in the host `fault_detector_msgs` repository.
